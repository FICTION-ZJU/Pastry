# models a probabilistic insertion sort
# deterministic version takes 
# 0.5max(0, n-2)max(0, n-1)
# probabilistic version takes 
# 3.max(0, n-2)max(0, n-1)

def f():
    var n, j, z

    while n > 0:
        j = n
        while j > 0:
        	# perform swapping with probability 1/3
            prob(1,2):
        	   j = j - 1
               tick 1
        # insert with probability 1/2
        n = n - unif(0,1)


